# Server Setup Generator

🚀 Автоматический генератор bash скриптов для настройки Linux серверов с веб-интерфейсом.

## Описание

Server Setup Generator - это веб-приложение для создания идемпотентных bash скриптов, которые автоматизируют настройку Linux серверов. Система поддерживает:

- Управление пользователями с SSH ключами
- Модульную архитектуру действий (установка ПО, настройки системы)
- Историю генераций с возможностью повторного использования
- Простое развертывание через Docker
- Полностью автономную работу без внешних зависимостей

## Возможности

### ✨ Основные функции

- **Управление пользователями**
  - Создание пользователей с заданными группами
  - Настройка SSH ключей (несколько ключей на пользователя)
  - Автоматическая генерация `.inputrc` для удобной работы в bash

- **Модульная система**
  - Гибкая архитектура с зависимостями между модулями
  - Поддержка параметризованных модулей (с пользовательским вводом)
  - Сортировка по приоритету выполнения
  - Предустановленные модули:
    - Установка Docker
    - Установка vim, net-tools, htop
    - Настройка sudoers (беспарольный sudo)
    - Отключение SSH аутентификации по паролю
    - Установка hostname и timezone

- **История генераций**
  - Сохранение всех конфигураций
  - Просмотр параметров и скриптов
  - Скачивание и повторное использование
  - Копирование curl команд

- **Веб-интерфейс**
  - Интуитивный UI на Bootstrap 5
  - Подсветка синтаксиса bash
  - Админ-панель для управления модулями
  - Responsive дизайн

## Быстрый старт

### Требования

- Docker
- Docker Compose

### Установка и запуск

```bash
# Клонировать репозиторий
git clone <repository-url>
cd server-setup-generator

# Запустить через Docker Compose
docker-compose up -d

# Проверить статус
docker-compose ps

# Просмотреть логи
docker-compose logs -f
```

Приложение будет доступно по адресу: **http://localhost:8080**

### Остановка

```bash
docker-compose down
```

### Полное удаление (включая данные)

```bash
docker-compose down -v
rm -rf data/ scripts/
```

## Использование

### 1. Добавление пользователей

1. На главной странице нажмите **"Add User"**
2. Заполните поля:
   - **Username**: имя пользователя (обязательно)
   - **Groups**: группы через запятую (например: `sudo,docker`)
   - **SSH Keys**: публичные SSH ключи (один на строку)
3. Нажмите **"Save"**

### 2. Выбор модулей

1. В секции **"Modules"** отметьте нужные действия
2. Для модулей с параметрами появятся поля ввода
3. Заполните параметры (например, hostname)

### 3. Системные настройки

- **Hostname**: имя сервера (опционально)
- **Timezone**: часовой пояс (по умолчанию UTC)

### 4. Генерация скрипта

1. Нажмите **"Generate Script"**
2. Скрипт появится в правой панели
3. Используйте один из способов:
   - Копировать скрипт и выполнить вручную
   - Скачать файл .sh
   - Использовать curl команду для прямого выполнения

### 5. Выполнение скрипта на сервере

```bash
# Способ 1: через curl (рекомендуется)
curl -sL http://YOUR_SERVER:8080/scripts/<hash>.sh | bash

# Способ 2: скачать и выполнить
wget http://YOUR_SERVER:8080/scripts/<hash>.sh
chmod +x <hash>.sh
sudo ./<hash>.sh
```

⚠️ **Важно**: Скрипт должен выполняться с правами root!

## Архитектура

### Структура проекта

```
server-setup-generator/
├── docker-compose.yml      # Docker Compose конфигурация
├── Dockerfile              # Образ приложения
├── requirements.txt        # Python зависимости
├── README.md              # Документация
├── app/
│   ├── main.py            # Точка входа FastAPI
│   ├── models.py          # SQLAlchemy модели
│   ├── generator.py       # Логика генерации bash
│   ├── routes.py          # API endpoints
│   ├── static/            # Статические файлы
│   │   ├── css/
│   │   │   └── style.css
│   │   └── js/
│   │       ├── app.js
│   │       ├── admin.js
│   │       └── history.js
│   └── templates/         # HTML шаблоны
│       ├── index.html
│       ├── admin.html
│       ├── history.html
│       └── error.html
├── data/                  # SQLite база данных (volume)
└── scripts/               # Сгенерированные скрипты (volume)
```

### База данных

SQLite с следующими таблицами:

- **users** - Пользователи для создания на сервере
- **modules** - Доступные модули/действия
- **configurations** - История генераций
- **configuration_modules** - Связь конфигураций и модулей
- **configuration_users** - Связь конфигураций и пользователей

### API Endpoints

#### Users

- `GET /api/users` - Список всех пользователей
- `GET /api/users/{id}` - Получить пользователя
- `POST /api/users` - Создать пользователя
- `PUT /api/users/{id}` - Обновить пользователя
- `DELETE /api/users/{id}` - Удалить пользователя

#### Modules

- `GET /api/modules` - Список всех модулей
- `GET /api/modules/{id}` - Получить модуль
- `POST /api/modules` - Создать модуль
- `PUT /api/modules/{id}` - Обновить модуль
- `DELETE /api/modules/{id}` - Удалить модуль

#### Configurations

- `GET /api/configurations` - История конфигураций
- `GET /api/configurations/{id}` - Получить конфигурацию
- `DELETE /api/configurations/{id}` - Удалить конфигурацию

#### Generation

- `POST /api/generate` - Сгенерировать скрипт

#### Scripts

- `GET /api/scripts/{hash}.sh` - Скачать скрипт

## Управление модулями

### Админ-панель

Доступна по адресу: **http://localhost:8080/admin**

Здесь можно:
- Добавлять новые модули
- Редактировать существующие
- Изменять приоритет выполнения
- Включать/отключать модули
- Настраивать зависимости

### Создание нового модуля

1. Откройте админ-панель
2. Нажмите **"Add Module"**
3. Заполните поля:
   - **Name**: название модуля
   - **Description**: описание
   - **Bash Code**: код для выполнения
   - **Priority**: порядок выполнения (меньше = раньше)
   - **Dependencies**: зависимости (через запятую)
   - **Requires Input**: требуется ли ввод пользователя
   - **Input Label/Default**: параметры для ввода

### Пример модуля с параметром

```json
{
  "name": "Install Node.js",
  "description": "Install Node.js with specific version",
  "bash_code": "curl -fsSL https://deb.nodesource.com/setup_{input}.x | bash -\napt-get install -y nodejs",
  "priority": 15,
  "requires_input": true,
  "input_label": "Node.js Version",
  "input_default": "20"
}
```

## Расширение функциональности

### Добавление новых типов модулей

Система поддерживает расширение через:

1. **Пользовательские модули** - создавайте через админ-панель
2. **Программные модули** - добавьте в `models.py` → `init_default_modules()`
3. **Внешние модули** - импорт/экспорт через API (будущая функция)

### Интеграция с CI/CD

```bash
# Пример использования в CI/CD
SCRIPT_URL="http://generator:8080/scripts/abc123.sh"
curl -sL $SCRIPT_URL | ssh root@target-server bash
```

## Безопасность

### Рекомендации

- ✅ Используйте HTTPS в продакшене (добавьте reverse proxy)
- ✅ Ограничьте доступ к веб-интерфейсу (firewall, VPN)
- ✅ Регулярно делайте бэкапы данных (`data/` и `scripts/`)
- ✅ Проверяйте сгенерированные скрипты перед выполнением
- ⚠️ Не храните секреты в модулях (используйте переменные окружения)

### Добавление HTTPS

```yaml
# docker-compose.yml
services:
  nginx:
    image: nginx:alpine
    ports:
      - "443:443"
    volumes:
      - ./nginx.conf:/etc/nginx/nginx.conf
      - ./certs:/etc/nginx/certs
    depends_on:
      - web
```

## Резервное копирование

### Создание бэкапа

```bash
# Остановить контейнер
docker-compose stop

# Создать архив
tar -czf backup-$(date +%Y%m%d).tar.gz data/ scripts/

# Запустить контейнер
docker-compose start
```

### Восстановление

```bash
# Остановить контейнер
docker-compose stop

# Распаковать бэкап
tar -xzf backup-20240101.tar.gz

# Запустить контейнер
docker-compose start
```

## Разработка

### Локальная разработка

```bash
# Установить зависимости
pip install -r requirements.txt

# Запустить сервер
cd app
python main.py

# Приложение доступно на http://localhost:8080
```

### Структура кода

- `models.py` - Модели данных (SQLAlchemy)
- `generator.py` - Логика генерации bash скриптов
- `routes.py` - REST API endpoints (FastAPI)
- `main.py` - Инициализация приложения
- `static/` - Frontend ресурсы
- `templates/` - Jinja2 шаблоны

### Технологии

- **Backend**: Python 3.11, FastAPI, SQLAlchemy
- **Frontend**: HTML5, CSS3, JavaScript, Bootstrap 5
- **Database**: SQLite
- **Container**: Docker, Docker Compose

## Troubleshooting

### Проблема: Контейнер не запускается

```bash
# Проверить логи
docker-compose logs web

# Пересобрать образ
docker-compose build --no-cache
docker-compose up -d
```

### Проблема: База данных не создается

```bash
# Проверить права доступа
ls -la data/

# Создать директорию вручную
mkdir -p data scripts
chmod 777 data scripts
```

### Проблема: Модули не загружаются

```bash
# Пересоздать базу данных
docker-compose down
rm -rf data/
docker-compose up -d
```

## Лицензия

MIT License

## Поддержка

Если у вас возникли вопросы или проблемы:

1. Проверьте документацию выше
2. Изучите логи: `docker-compose logs -f`
3. Создайте issue в репозитории

## Автор

Created with ❤️ for DevOps engineers

---

**Happy scripting! 🚀**
